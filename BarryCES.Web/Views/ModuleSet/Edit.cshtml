@model BarryCES.Models.ModuleDto

@{
    ViewBag.Title = "编辑板块";
    Layout = "~/Views/Shared/_Layout_Content.cshtml";
}

<div class="wrapper wrapper-content">
    <div class="ibox float-e-margins">
        <div class="ibox-title">
            <h5>编辑板块</h5>
            <div class="ibox-tools">
                <a class="collapse-link">
                    <i class="fa fa-chevron-up"></i>
                </a>
            </div>
        </div>
        <div class="ibox-content">
            @using (Html.BeginForm("Edit", "ModuleSet", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.HiddenFor(m => m.Id)
                @Html.HiddenFor(m => m.ParentId)
                <div class="form-group">
                    <label class="col-sm-3 control-label">@Html.LabelFor(m => m.ModuleName)：</label>
                    <div class="col-sm-8">
                        @Html.TextBoxFor(m => m.ModuleName, new { @class = "form-control", placeholder = "板块名称" })
                        @Html.ValidationMessageFor(m => m.ModuleName)
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label">@Html.LabelFor(m => m.Order)：</label>
                    <div class="col-sm-8">
                        @Html.TextBoxFor(m => m.Order, new { @class = "form-control", placeholder = "排序" })
                        @Html.ValidationMessageFor(m => m.Order)
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-3 col-sm-8">
                        <button class="btn btn-info " type="submit">保存</button>
                        <button class="btn btn-white " type="button" onclick="window.location.href='@Url.Action("Index")'">返回</button>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
@section scripts{
    @Scripts.Render("~/content/js/jqueryValidate", "~/content/js/moduleset")
    <script>
            $("#btnBack").bind("click", function(e) {
                XPage.GoTo(e.target, "@Url.Action("Index")");
            });
    </script>
}